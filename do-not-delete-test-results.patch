diff --git a/t/Makefile b/t/Makefile
index 43b15e3..3c84640 100644
--- origsrc/git/t/Makefile
+++ src/git/t/Makefile
@@ -46,8 +46,13 @@ $(T):
 pre-clean:
 	$(RM) -r '$(TEST_RESULTS_DIRECTORY_SQ)'
 
+# Don't delete the test results directory immediately after running the tests!
+# That directory's useful!
+#clean-except-prove-cache:
+#	$(RM) -r 'trash directory'.* '$(TEST_RESULTS_DIRECTORY_SQ)'
+#	$(RM) -r valgrind/bin
 clean-except-prove-cache:
-	$(RM) -r 'trash directory'.* '$(TEST_RESULTS_DIRECTORY_SQ)'
+	$(RM) -r 'trash directory'.*
 	$(RM) -r valgrind/bin
 
 clean: clean-except-prove-cache
